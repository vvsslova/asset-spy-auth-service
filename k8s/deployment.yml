apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: asset-spy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: vvsslova/asset-spy-auth-service:latest
          envFrom:
            - configMapRef:
                name: auth-service-configmap
            - secretRef:
                name: auth-service-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "150m"
            limits:
              memory: "384Mi"
              cpu: "300m"
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms256m -Xmx384m"
          ports:
            - containerPort: 8085